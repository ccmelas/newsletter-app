# Newsletter Subscription Laravel App

This application follows Laravel Repository Pattern and touches on Dependency Injection.

To quickly use this repo, clone it and in the project folder, run:

```
composer install
```
